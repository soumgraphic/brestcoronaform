import { FrenchDateFormatPipe } from './french-date-format.pipe';

describe('FrenchDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new FrenchDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
